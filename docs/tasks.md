# 作業の一覧（第1版・第1.1版・第1.2版・第1.3版）

- 状態：`[ ]` 未着手 / `[~]` 作業中 / `[x]` 完了
- 計算ロジック（E）→ 保存（S）→ 画面（U）の順に積む。1つの作業は半日以内
- 設計は `docs/architecture.md`。どの作業も完了の条件に加えて `npm run check` が通ること（画面の作業は `npm run build` も）
- 「見本」は E-02 の本棚 W900 のデータを指す

## 見本（本棚 W900）で期待する値

| 部材 | 板 | 仕上がり寸法 W×H×D | 厚みの寸法 | 木取り寸法（面） | 枚数 |
|---|---|---|---|---|---|
| 全体 | なし | 900×1800×400 | － | － | 0 |
| 側板 | シナランバー 18 | 18×1800×400 | W | 1810×410（H×D） | 2 |
| 天地板 | シナランバー 18 | 864×18×400 | H | 874×410（W×D） | 2 |
| 棚板 | シナランバー 18 | 863×18×380（逃げ W1。第1.1版からは W = `天地板.W − 逃げ1mm`） | H | 873×390（W×D） | 4 |
| 背板 | シナベニヤ 4 | 900×1800×4（切り代 0） | D | 900×1800（W×H） | 1 |

- 設定：刃厚 3、耳落とし 5、切り代 10、縦切り優先。板はどちらもサブロク 910×1820、木目は長辺方向
- 縦切り優先の結果：シナランバー 18 は **3枚**（1枚目 側板×2／2枚目 天地板×2＋棚板×2／3枚目 棚板×2）、シナベニヤ 4 は **1枚**
- 横切り優先（横長に置き、上の長手と右の妻手を端切り）でも シナランバー 18 は 3枚、シナベニヤ 4 は 1枚。歩留まりも縦切り優先と同じ
- 歩留まり（板全体の面積に対する木取り寸法の面積。暫定の定義）：ランバー 1枚目 89.6%、2枚目 84.4%、3枚目 41.1%、ランバー全体 71.7%、ベニヤ 97.8%、全体 78.2%

---

## 計算ロジック（engine）

### [x] E-01 データ型と初期値
- 担当：engine-dev ／ 依存：なし ／ 仕様書：4, 5.1, 5.2
- やること：`src/engine/types.ts` に architecture.md 2章の型（Settings, Board, Part, Job, PartDimensions, DimensionResult, Rect, Placement, CutStep, SheetLayout, MaterialResult, PackingResult など）と `DEFAULT_SETTINGS`・`BOARD_SIZES` を定義する
- 完了の条件：`DEFAULT_SETTINGS` が 刃厚3・耳落とし5・切り代10・縦切り優先、`BOARD_SIZES` がサブロク 910×1820・シハチ 1220×2440 であるテストが通る

### [x] E-02 見本データ（本棚 W900）
- 担当：engine-dev ／ 依存：E-01 ／ 仕様書：5.2, 6
- やること：`src/engine/fixtures/bookshelf.ts` に見本の Job を作る。全体 W900 H1800 D400 枚数0 ／ 側板 W18 H`全体.H` D`全体.D` 枚数2 木目H ／ 天地板 W`全体.W - 側板.W * 2` H18 D`全体.D` 枚数2 木目W ／ 棚板 W`天地板.W` H18 D`全体.D - 20` 枚数4 木目W 逃げW1 ／ 背板 W`全体.W` H`全体.H` D4 シナベニヤ4 枚数1 木目H 切り代0。板はシナランバー18・シナベニヤ4（サブロク、木目 長辺方向）。設定は初期値
- 完了の条件：見本が Job 型として型チェックを通り、部材5つ・板2つ・部材名の重複なしを確かめるテストが通る

### [x] E-03 式の字句の切り出し
- 担当：engine-dev ／ 依存：E-01 ／ 仕様書：6
- やること：`formula/tokenize.ts`。数値（小数可）・参照（`部材名.W/H/D`）・`+ - * / ( )` に分ける
- 完了の条件：`全体.W - 側板.W * 2` が［参照(全体,W), -, 参照(側板,W), *, 数値2］になる、`12.5` が数値、`天地板.X` や `@` が字句のエラーになるテストが通る

### [x] E-04 式の構文解析と計算
- 担当：engine-dev ／ 依存：E-03 ／ 仕様書：6
- やること：`formula/parse.ts`・`evaluate.ts`。四則演算の優先順位・括弧・符号の `-` に対応。参照の一覧を取り出す関数と、参照の値を渡して計算する関数を作る。例外は投げず結果の型で返す
- 完了の条件：`(900 - 18 * 2) / 2` = 432、`-5 + 10` = 5、`1 +` と `(1` が構文エラー、`10 / 0` が 0 除算エラー、`全体.W - 側板.W * 2` の参照一覧が［全体.W, 側板.W］になるテストが通る

### [x] E-05 参照の依存関係（存在しない部材・循環参照）
- 担当：engine-dev ／ 依存：E-04 ／ 仕様書：6
- やること：`dimensions/resolve.ts`。全部材の W・H・D の参照から計算順を求め、存在しない部材名と循環参照を見つける
- 完了の条件：見本の計算順で 全体 が 天地板 より先、天地板.W が 棚板.W より先になる。`天板.W` を参照して天板が無いと unknownRef（部材名「天板」を示す）、A.W=`B.W`・B.W=`A.W` と自分自身の参照（A.W=`A.W`）が cycle になるテストが通る

### [x] E-06 仕上がり寸法（式の計算・逃げ・0以下のエラー）
- 担当：engine-dev ／ 依存：E-05 ／ 仕様書：6, 7
- やること：`dimensions/finished.ts`。計算順に式を計算し、面の2軸から逃げを引く。参照先の値は逃げを引いた後の仕上がり寸法。0以下はエラー。エラーのある寸法を参照している寸法は計算しない
- 完了の条件：見本で 天地板 W=864、棚板 W=863（逃げ1を引いた値）・D=380 になる。棚板.W を参照する部材は 863 を受け取る。`全体.W - 1000` が nonPositive になるテストが通る

### [x] E-07 厚みの寸法の判定
- 担当：engine-dev ／ 依存：E-06 ／ 仕様書：5.3
- やること：`dimensions/thickness.ts`。板の厚みと同じ値の軸を自動で選ぶ。手で選んだ軸を優先。値が板の厚みと違えば不一致の印を付ける。面の2軸を決める
- 完了の条件：見本で 側板＝W、天地板・棚板＝H、背板＝D が自動で選ばれる。W18 H18 D600 の部材は W が選ばれる。手で D を選んだ W18 H700 D600 の部材が不一致になる。どの軸も厚みと合わない部材は null（不一致の印あり）になるテストが通る

### [x] E-08 木取り寸法と寸法表のまとめ
- 担当：engine-dev ／ 依存：E-07 ／ 仕様書：7, 9（寸法表）
- やること：`dimensions/cutSize.ts`・`index.ts`。面の2軸に切り代を足す（部材ごとの上書き、0 も有効）。`computeDimensions(job): DimensionResult` で部材ごとの仕上がり・木取り寸法・エラーをまとめる
- 完了の条件：見本で 側板 1810×410、天地板 874×410、棚板 873×390、背板 900×1800（切り代0）になり、厚みの軸の値は変わらない。仕様書の例 天板 W900×D600・切り代10 → 910×610 のテストが通る

### [x] E-09 片の展開と木目による向き
- 担当：engine-dev ／ 依存：E-08 ／ 仕様書：8
- やること：`packing/pieces.ts`。枚数ぶんの片に展開し、板ごとに分ける。板の木目（長辺／短辺）に部材の木目の軸を合わせて x・y を決める。「どちらでもよい」は回転可。枚数0・寸法エラー・板なしの部材は除き、理由を返す。どう置いても入らない片は unplaced
- 完了の条件：見本でランバーの片が8枚・ベニヤが1枚になる。側板は y=1810・x=410、天地板は y=874・x=410。板の木目が短辺方向なら天地板は x=874 になる。全体は除かれる。1000×2000 の部材がサブロクで unplaced になるテストが通る

### [x] E-10 板の使える範囲と刃厚込みの入り判定
- 担当：engine-dev ／ 依存：E-01 ／ 仕様書：4, 8
- やること：`packing/sheet.ts`。縦長に置いた板の使える範囲（右の長辺から耳落とし）と、長さ L に片を刃厚込みで並べられるかの判定
- 完了の条件：サブロク・耳落とし5 の使える範囲が x 0〜905・y 0〜1820。L=823 に 410 と 410 が入り（410+3+410）、L=822 には入らない。L=905 に 905 が1枚ぴったり入るテストが通る

### [x] E-11 縦切り優先の配置（右から帯詰め）
- 担当：engine-dev ／ 依存：E-09, E-10 ／ 仕様書：4（切り方）, 8
- やること：`packing/guillotine.ts`。architecture.md 3.3 の帯詰め。最初の帯は使える範囲の右端に接し、以降は左へ。入らなければ新しい板
- 完了の条件：見本でシナランバー18が3枚（1枚目 側板×2、2枚目 天地板×2＋棚板×2、3枚目 棚板×2）、シナベニヤ4が1枚。すべての片が使える範囲の中にあり、片どうしが重ならず、隣り合う片の間が3mm以上あく。1枚目の最初の側板の右端が x=905 になるテストが通る

### [x] E-12 横切り優先の配置
- 担当：engine-dev ／ 依存：E-11 ／ 仕様書：4（切り方）, 8
- やること：E-11 の処理を x・y を入れ替えて使い、短辺方向の帯で配置する
- 完了の条件：見本でシナランバー18が3枚・シナベニヤ4が1枚。E-11 と同じ「範囲内・重ならない・刃厚あき・右から詰める」のテストが通る

### [x] E-13 切る順番
- 担当：engine-dev ／ 依存：E-11, E-12 ／ 仕様書：9（切る順番）
- やること：`packing/cutOrder.ts`。耳落とし → 帯を右から切り離す → 帯を片に切り分ける → 幅の切り揃え の順で CutStep を作り、日本語のラベルを付ける
- 完了の条件：見本・縦切り優先の1枚目で、1番目が耳落とし（縦、x=905）、2番目が右端から410mmの縦の切断になる。どの切断も切る範囲の端から端まで一直線（ギロチン）であることを確かめるテストが通る

### [x] E-14 端材
- 担当：engine-dev ／ 依存：E-11, E-12 ／ 仕様書：9（端材）
- やること：`packing/scraps.ts`。板の左の残り・帯の残り・細い片の横の残りを、刃厚を除いた長方形で返す
- 完了の条件：見本・縦切り優先の1枚目の端材が 左側 x 0〜79（幅79×1820）だけになる（各帯の下の 410×7 は 30mm 未満なので出さない：未決事項10、帯の中は上から詰める：未決事項9）。端材が片や耳落としと重ならないテストが通る

### [x] E-15 歩留まり
- 担当：engine-dev ／ 依存：E-11 ／ 仕様書：9（歩留まり）
- やること：`packing/yield.ts`。板ごと・材料ごと・全体の歩留まり
- 完了の条件：見本・縦切り優先でランバー 1枚目 89.6%、2枚目 84.4%、3枚目 41.1%、ランバー全体 71.7%、ベニヤ 97.8%、全体 78.2%（小数第1位）になるテストが通る

### [x] E-16 木取り計算の入口とおまかせ
- 担当：engine-dev ／ 依存：E-13, E-14, E-15 ／ 仕様書：4（切り方）, 8, 9
- やること：`packing/index.ts` の `packJob(job, dims): PackingResult`。材料ごとに切り方に従って計算。おまかせは両方を計算して、必要枚数が少ないほう → 一番大きい端材が大きいほう → 縦切り優先（未決事項2）
- 完了の条件：見本で PackingResult の材料が2つ・必要枚数 3 と 1。おまかせで選んだ切り方が結果に入る。部材120枚（10種類）のデータで 1秒以内に終わるテストが通る

### [x] E-17 横切り優先を横長の置き方に作り直す
- 担当：engine-dev ／ 依存：E-12〜E-16 ／ 仕様書：4（切り方）, 8
- やること：横切り優先は材料を横長に置き（長手が横、妻手が右）、上の長手 → 右の妻手の順に端切りして矩を出す。右から妻手の幅いっぱいの帯を切り出し、帯の中は上から詰める（縦切り優先と同じ処理を横長の板に行う）。結果に置き方（`orientation`）と端切りの範囲（`trims`）を入れる
- 完了の条件：見本・横切り優先でシナランバー18が3枚・シナベニヤ4が1枚、1枚目の側板が x 5〜1815・y 495〜905 と y 82〜492。切る順番が 端切り（上の長手）→ 端切り（右の妻手）→ 右端から 1810mm で縦 → 上端から 410mm で横 ×2。横長でもギロチンで片がすべて切り出せるテストが通る

## 保存（store）

### [x] S-01 仕事の操作（純粋関数）
- 担当：ui-dev ／ 依存：E-01 ／ 仕様書：5.1, 5.2, 10
- やること：`src/store/jobs.ts`。仕事の作成、部材の追加・変更・削除（名前の重複を拒否）、板の追加・変更・削除（材料名＋厚みの重複を拒否）、板を使っている部材名の一覧
- 完了の条件：新しい仕事の設定が初期値。同じ名前の部材を足すとエラー。見本でシナランバー18を使っている部材が［側板, 天地板, 棚板］と返り、その板を削除すると3部材の板が未設定になるテストが通る

### [x] S-02 localStorage への保存と読み込み
- 担当：ui-dev ／ 依存：S-01 ／ 仕様書：10
- やること：`src/store/storage.ts`。仕事の一覧と開いている仕事の id の読み書き。すべて try/catch。壊れたデータは読まずに知らせる（上書きしない）
- 完了の条件：見本を保存して読み込むと同じ内容に戻る。localStorage が例外を投げても、壊れた JSON が入っていても落ちずに空の一覧とエラーの印を返すテストが通る

### [x] S-03 状態管理（Context と自動保存）
- 担当：ui-dev ／ 依存：S-02 ／ 仕様書：10
- やること：`src/store/JobStore.tsx`。useReducer で仕事を持ち、変わったら少し待って保存する。画面から使うフックを用意する
- 完了の条件：部材を変えて再読み込みしても内容が残ることを、開発サーバーで確かめる。reducer の操作のテストが通る

## 画面（ui）

### [x] U-01 画面の骨組み（下のタブ）
- 担当：ui-dev ／ 依存：S-03 ／ 仕様書：1, 9
- やること：`src/ui/App.tsx`。仕事・部材・寸法表・木取り・設定 の5つのタブ。仕事を開いていないときは仕事以外を押せない。ライト／ダークの配色の土台
- 完了の条件：幅375pxで横スクロールが出ず、タブの高さが44px以上。タブを押すと画面が切り替わる

### [x] U-02 仕事の画面
- 担当：ui-dev ／ 依存：U-01 ／ 仕様書：10
- やること：保存した仕事の一覧（名前・更新日）、新しく作る、開く
- 完了の条件：仕事を2つ作り、再読み込み後も一覧に残り、どちらも開き直せる

### [x] U-03 設定の画面（刃厚・耳落とし・切り代・切り方）
- 担当：ui-dev ／ 依存：U-01 ／ 仕様書：4
- やること：刃厚・耳落とし・切り代（よく使う値のボタン＋数値入力）・切り方（縦切り優先／横切り優先／おまかせ）の変更
- 完了の条件：新しい仕事で 3・5・10・縦切り優先 が出る。値を変えて再読み込みしても残る

### [x] U-04 設定の画面（板の追加・削除）
- 担当：ui-dev ／ 依存：U-03 ／ 仕様書：5.1
- やること：板の一覧・追加・編集・削除。サイズはサブロク／シハチ／自由入力、自由入力のときは木目（長辺・短辺）を選べる。部材が使っている板を消すときは部材名を示して確認する
- 完了の条件：見本の板2つを画面から登録できる。同じ材料名＋厚みは登録できない。部材が使っている板を消そうとすると部材名入りの確認が出る

### [x] U-05 部材の画面（一覧と編集の基本）
- 担当：ui-dev ／ 依存：U-04, E-08 ／ 仕様書：5.2, 5.3
- やること：部材カードの一覧と編集シート。名前・板・W/H/D（数値）・厚みの寸法（自動／W／H／D）・枚数・木目（面の2軸＋どちらでもよい）・逃げ（面の2軸）・切り代（空欄＝初期値）。厚み不一致の注意表示
- 完了の条件：見本の5部材を数値で入力でき、木目と逃げの選択肢が厚みの寸法に応じて変わる（側板なら H・D）。名前の重複は保存できない。枚数1以上で板を選んでいない部材のカードに「板が未設定」と出る（枚数0の行には出さない）

### [x] U-06 式の入力（参照ボタンと演算子ボタン）
- 担当：ui-dev ／ 依存：U-05 ／ 仕様書：5.4, 6
- やること：`FormulaInput.tsx`。入力欄の下に登録済みの部材の寸法ボタン（`全体.W` など）、`+ − × ÷ ( )`、数字。押すとカーソル位置に入る。キーボードでも打てる
- 完了の条件：キーボードを使わずに 天地板 W＝`全体.W - 側板.W * 2` を作れる。見本の式をすべてボタンだけで入力できる

### [x] U-07 式のエラーの表示
- 担当：ui-dev ／ 依存：U-06 ／ 仕様書：6
- やること：存在しない部材・循環参照・0以下・読めない式を、該当の欄の下と部材カードに日本語で出す
- 完了の条件：`天板.W` を参照すると「天板という部材はありません」相当、A↔B の参照で循環参照、`全体.W - 1000` で0以下 のエラーがそれぞれ出る。参照先を直すと消える

### [x] U-08 寸法表の画面
- 担当：ui-dev ／ 依存：U-05 ／ 仕様書：9（寸法表）
- やること：部材ごとのカード。仕上がり寸法と木取り寸法を別の段に分け、見出し・色・並びで区別。枚数・厚みの寸法・厚み不一致の注意
- 完了の条件：見本で幅375pxに横スクロールなしで表示され、棚板のカードに 仕上がり 863×18×380 と 木取り 873×390 が別の段・別の色で出る。ダーク配色でも読める。枚数1以上で板を選んでいない部材のカードに「板が未設定」と出る（枚数0の行には出さない）

### [x] U-09 木取りの画面（まとめ）
- 担当：ui-dev ／ 依存：U-01, E-16 ／ 仕様書：9
- やること：材料ごとの必要な板の枚数・歩留まり・使った切り方、全体の歩留まり、入らない部材・計算できない部材の一覧
- 完了の条件：見本で シナランバー18 3枚・シナベニヤ4 1枚、全体の歩留まり 78.2% が出る

### [x] U-10 配置図（SVG）
- 担当：ui-dev ／ 依存：U-09 ／ 仕様書：9（配置図）
- やること：`SheetDiagram.tsx`。板ごとに部材（名前・寸法）・耳落とし・端材を色分けして描く。板を縦長に置き、耳落としが右
- 完了の条件：見本の1枚目で側板2枚が右側に並び、左に端材が見える。幅375pxで板全体が画面に収まり、部材名が読める

### [x] U-11 切る順番と端材の表示
- 担当：ui-dev ／ 依存：U-10, E-13, E-14 ／ 仕様書：9（切る順番・端材）
- やること：板ごとに切る順番の一覧（番号・ラベル）と端材のサイズ一覧
- 完了の条件：見本の1枚目で、1番が耳落とし・2番が右端から410mmの縦の切断と出る。端材 79×1820 が一覧に出る

### [x] U-12 ホーム画面に追加できるようにする
- 担当：ui-dev ／ 依存：U-01 ／ 仕様書：1
- やること：Web アプリの manifest とアイコンを置き、ホーム画面に追加したときにアプリ名とアイコンが出るようにする（base は `/kidori/`）。オフライン対応は未決事項のため含めない
- 完了の条件：`npm run build` の出力に manifest が含まれ、開発者ツールで manifest が読めている

---

## 第1.1版（実機で使ってみての改善）

- 設計は `docs/architecture.md` の 6章。計算ロジック（E）→ 保存（S）→ 画面（U）の順に積む
- 見本の棚板は、部材ごとの逃げ（W1）をやめて W = `天地板.W − 逃げ1mm` にする（E-21）。見本で期待する値（上の表）は変わらない
- 型を変えたために `src/store`・`src/ui` の型チェックが通らなくなる箇所は、その作業の中で最小限だけ直す（初期値を入れる・使わなくなった欄を外す）。画面の作り込みは U の作業で行う

### 計算ロジック（engine）

### [x] E-18 データ型と初期値（逃げ・メモ・加工のチェック・初期の材料）
- 担当：engine-dev ／ 依存：なし ／ 仕様書：4（逃げ）, 5.1, 5.2, 9（加工のチェック）
- やること：architecture.md 6.2。`Nige` 型、`Settings.nige`、`Part.memo`・`Part.checks`、`DimensionErrorKind` に `missingBoard`・`missingNige` を足す。`src/engine/defaults.ts` に `defaultNige()`・`defaultBoards(newId)`・`nigeName(value)`・`boardTokenLabel(board)` を作る。見本に `nige: defaultNige()`・`memo: ''`・`checks` を入れる（`clearance` はこの作業では残す）
- 完了の条件：`defaultNige()` が 0.5 と 1 の2つで、名前が「逃げ0.5mm」「逃げ1mm」になる。呼ぶたびに別の配列になる。`defaultBoards` が メラミン 1・ラワン 2.5・ラワン 4・ラワン 5.5 の4つ（すべて 4×8 1220×2440・木目 長手方向・id が別々）。`boardTokenLabel` が「ラワン4mm」を返すテストが通る

### [x] E-19 式の字句：材料の厚み・逃げ
- 担当：engine-dev ／ 依存：E-18 ／ 仕様書：5.4, 6
- やること：architecture.md 6.3「字句」。`{t:板のid}`・`{n:逃げのid}` を1つの字句として読む。部材名に `{` `}`（全角も）を使えなくする。部材名のつけ替えで `{…}` を書き換えない
- 完了の条件：`天地板.W - {n:nige-1}` が［参照(天地板,W), -, 逃げ(nige-1)］、`600 - {t:b-4} * 2` が［数値600, -, 厚み(b-4), *, 数値2］になる。`{x:1}` と `{n:nige-1`（閉じていない）が字句のエラー。部材名「棚{1}」が使えない。部材名を変えても式の中の `{n:nige-1}` が変わらないテストが通る

### [x] E-20 式の計算：材料の厚み・逃げの値
- 担当：engine-dev ／ 依存：E-19 ／ 仕様書：6
- やること：architecture.md 6.3「構文木と計算」。構文木に厚み・逃げを足し、`evaluate` の lookup を `{ ref, thickness, nige }` に広げる。`computeDimensions` が `job.boards` の厚みと `job.settings.nige` の寸法を渡す。見つからなければ `missingBoard`・`missingNige`
- 完了の条件：ラワン 4mm の板（id `b-4`）がある仕事で、D = `600 - {t:b-4} * 2` が 592 になり、その板の厚みを 5.5 に変えると 589 になる。逃げ1mm（id `nige-1`）で W = `900 - {n:nige-1}` が 899、逃げの寸法を 0.5 に変えると 899.5 になる。逃げを設定から消すと missingNige（「削除した逃げを使っています」）になり、その寸法を参照している部材もエラーになる。板を消すと missingBoard になるテストが通る

### [x] E-21 部材ごとの逃げをなくす（仕上がり寸法 = 式の計算結果）
- 担当：engine-dev ／ 依存：E-20 ／ 仕様書：4（逃げ）, 7
- やること：architecture.md 6.4。`Part.clearance` を型から消し、`finished.ts` の逃げと厚みの判定まわりの処理を消して単純にする。見本の棚板を W = `天地板.W - {n:nige-1}` に書き換える。逃げを前提にしたテストは、式で引く形に書き直すか消す。store・ui の `clearance` を使う箇所は外す（部材の編集の逃げの欄も外す）
- 完了の条件：見本で 棚板 仕上がり 863×18×380・木取り 873×390、ほかの部材の値も上の「見本で期待する値」の表のとおり。縦切り優先でシナランバー 18 が 3枚・シナベニヤ 4 が 1枚、歩留まりも表のとおり。`src` の中に `clearance` が残っていない（移し替えの E-23 を除く）。`npm run check` が通る

### [x] E-22 式の単位・表示名・使っている部材・id のつけ替え
- 担当：engine-dev ／ 依存：E-19 ／ 仕様書：4（逃げ）, 5.1, 5.4
- やること：architecture.md 6.3。`formula/units.ts` の `formulaUnits(expr)`（部材の参照・厚み・逃げは1つの単位、数字は1字ずつ、空白は単位にしない）。`formula/display.ts` の `unitLabel(unit, job)`。`formula/usages.ts` の `partsUsingNige(job, id)`・`partsUsingBoardThickness(job, id)`・`remapBoardIds(expr, map)`
- 完了の条件：`全体.W - {n:nige-1} * 12` の単位が［全体.W, -, 逃げ, *, 1, 2］の6つになる。表示名が `全体.W`・`−`・`逃げ1mm`・`×`・`1`・`2`、消した逃げは `（削除した逃げ）`、厚みは `ラワン4mm` になる。見本で `partsUsingNige(job, 'nige-1')` が［棚板（W）］、`partsUsingNige(job, 'nige-0.5')` が空。`remapBoardIds` で `{t:a}` が `{t:b}` になり、ほかの部分が変わらないテストが通る

### [x] E-23 以前の版の部材ごとの逃げを移し替える
- 担当：engine-dev ／ 依存：E-21 ／ 仕様書：10
- やること：architecture.md 6.5。`src/engine/migrate/clearance.ts` の `migrateClearance(legacyJob, makeId)`
- 完了の条件：以前の形の見本（棚板 W = `天地板.W`・逃げ W1、設定に逃げなし）を移し替えると、設定の逃げが 0.5・1 の2つになり、棚板 W が `天地板.W - {n:nige-1}` になり、仕上がり 863 のまま。逃げ W2 の部材は 逃げ2mm が足され、式が `(全体.W - 36) - {n:…}` のように括弧つきになる。厚みの寸法の軸に入っていた逃げは式に足さない。移し替えた結果をもう一度移し替えても変わらないテストが通る

### [x] E-24 材料を減らせるときのお知らせ
- 担当：engine-dev ／ 依存：E-21 ／ 仕様書：9（材料を減らせるときのお知らせ）
- やること：architecture.md 6.7。`src/engine/hints/saving.ts` の `findSavingHints(job): SavingHint[]`。候補は 切り代 0・5・10 のうち今より小さい値（仕事の切り代だけ変える）と 端切り 0。1つずつ変えて計算し直し、必要枚数が減る材料を知らせる
- 完了の条件：サブロク・端切り5・刃厚3・切り代10・縦切り優先で、W445 H1800 D18（厚み D、木目 H）×4枚 の部材だけの仕事が 4枚になり、お知らせに「切り代を 5mm にすると、シナランバー 18mm が 2 枚減ります（4枚 → 2枚）」が出る（切り代 0 のお知らせは同じ内容なので出ない。端切り 0 のお知らせは出ない）。切り代0・W453 H1800 D18 ×2枚 の仕事で「端切りを 0mm にすると、シナランバー 18mm が 1 枚減ります（2枚 → 1枚）」が出る。見本（初期設定）ではお知らせが出ない。計算の前後で仕事のデータが変わらないテストが通る

### [x] E-25 材料を減らせるときのお知らせ：切り代を優先し、1mm 刻み・最後に 0.5mm で試して一番大きい値を出す
- 担当：engine-dev ／ 依存：E-24 ／ 仕様書：9（材料を減らせるときのお知らせ）
- やること：architecture.md 6.7。`findSavingHints(job)` で、まず切り代を今の値から 1mm ずつ小さくして 1mm まで・最後に 0.5mm で試す（0mm は試さない。今の値が小数なら切り捨てた値から）。切り代で減らなかった材料だけ端切りを同じように試す。材料ごとに、必要枚数が減る一番大きい値を知らせる。試す値の引数（`allowanceCandidates`・`trimCandidates`）はなくす
- 完了の条件：E-24 の例（W445 ×4・切り代10）で「切り代を 6mm にすると、シナランバー 18mm が 2 枚減ります（4枚 → 2枚）」が出る（451+3+451=905 ぴったり。7mm は入らない）。W444 ×2・切り代8 で 7mm が出て、端切りのお知らせは出ない。W452 ×2・切り代1 で「端切りを 1mm にすると…」だけが出る。0mm でしか入らないときは何も出ない。部材150枚・端切り5・切り代10・おまかせで数秒以内に終わる

### 保存（store）

### [x] S-04 仕事の操作：初期の材料と逃げ・逃げの追加と削除・メモとチェック
- 担当：ui-dev ／ 依存：E-18, E-22 ／ 仕様書：4（逃げ）, 5.1, 5.2, 9（加工のチェック）
- やること：`src/store/jobs.ts`。`createJob` に `defaultBoards`・`defaultNige` を入れる。逃げの追加（寸法だけ、0 より大きい、同じ寸法は拒否）・寸法の変更（同じ寸法は拒否）・削除。`partsUsingBoard` の確認用に式で厚みを使っている部材も返す関数。`copyJob` で式の `{t:…}` を新しい板の id につけ替える。部材のメモ・チェックの変更
- 完了の条件：新しい仕事に材料4つ（メラミン 1・ラワン 2.5・4・5.5）と 逃げ0.5mm・逃げ1mm が入っている。逃げ1mm をもう1つ足すとエラー、逃げ2mm は足せる。見本の逃げ1mm の削除確認用に［棚板（W）］が返る。厚みを式で使った仕事をコピーすると、コピー先の式が新しい板の id を指し、同じ寸法になる。チェックを付けても寸法が変わらないテストが通る

### [x] S-05 保存データ第2版と、以前の版からの移し替え
- 担当：ui-dev ／ 依存：E-23, S-04 ／ 仕様書：10
- やること：architecture.md 6.6。キー `kidori.jobs.v2`（version 2）で読み書きする。v2 が無く v1 があれば読んで `migrateClearance` で移し替える。v1 は消さない・書き換えない。`settings.nige`・`memo`・`checks` の検査と修復
- 完了の条件：v1 の形で保存した見本（棚板に逃げ W1）を読み込むと、棚板の式が `天地板.W - {n:nige-1}` になって仕上がり 863 のまま、保存すると v2 に書かれ v1 は元のまま残る。v2 の見本を保存して読み込むと同じ内容に戻る。逃げの値が 0 や重複しているデータは直した印（repaired）つきで読める。localStorage が例外を投げても落ちないテストが通る

### 画面（ui）

### [x] U-13 式の入力をボタンだけにする（キーボードを出さない・カーソルの左右移動）
- 担当：ui-dev ／ 依存：E-22 ／ 仕様書：5.4
- やること：architecture.md 6.8。`FormulaInput.tsx` を `<input>` を使わない表示枠にし、キーボードを出さない。◀ ▶ でカーソルを動かし、部材の参照は1つの塊として動かす・消す。「全部消す」ボタン。`src/ui/formulaEdit.ts` をカーソル＝単位の番号で作り直す（`moveLeft`・`moveRight`・`insertAt`・`deleteBefore`・`clearAll`）
- 完了の条件：`formulaEdit` のテストで、`全体.W - 側板.W * 2` の末尾から ◀ を2回押して `側板.W` の前にカーソルが来る（塊を1回で越える）、そこで1字消すと `-` が消える、`12` の間にカーソルを置いて `3` を押すと `132` になる。iPhone の幅（375px）の開発サーバーで、式の欄を押してもキーボードが出ず、ボタンだけで 天地板 W＝`全体.W − 側板.W × 2` を作れる

### [x] U-14 式の入力に材料の厚み・逃げのボタンを足す
- 担当：ui-dev ／ 依存：U-13, S-04 ／ 仕様書：5.4, 6
- やること：式の入力の下に、材料の厚みのボタン（`ラワン4mm` など、材料の登録順）と逃げのボタン（`逃げ0.5mm` など、登録順）を足す。式の中では色つきの塊で表示する
- 完了の条件：ボタンだけで 棚板 W＝`天地板.W − 逃げ1mm` を作ると 863 になる。設定で逃げ1mm の寸法を 2 に変えると、式の表示が `天地板.W − 逃げ2mm` になり寸法表の棚板が 862 になる。材料の厚みのボタンを使った式が、材料の厚みを変えるとついてくる

### [x] U-15 設定の画面：逃げの一覧（追加・変更・削除）
- 担当：ui-dev ／ 依存：S-04 ／ 仕様書：4（逃げ）
- やること：逃げの一覧（`逃げ0.5mm` など）。寸法を入れるだけで追加（名前は自動）。寸法の変更。削除は、式で使っていれば部材名と軸を示して確認する
- 完了の条件：新しい仕事で 逃げ0.5mm・逃げ1mm が出る。2 を足すと 逃げ2mm が並び、1 をもう一度足そうとすると「逃げ1mm はすでにあります」相当が出る。見本で逃げ1mm を消そうとすると「棚板（W）」入りの確認が出て、消すと棚板の W に「削除した逃げを使っています」が出る。再読み込みしても残る

### [x] U-16 材料の削除の確認に、式で厚みを使っている部材も出す
- 担当：ui-dev ／ 依存：S-04 ／ 仕様書：5.1
- やること：材料を削除するときの確認に、その材料から切る部材に加えて、式でその材料の厚みを使っている部材（部材名と軸）も出す
- 完了の条件：ラワン 4mm の厚みを式で使っている部材がある仕事で、ラワン 4mm を消そうとするとその部材名が確認に出る。消すとその寸法に「削除した材料の厚みを使っています」が出る

### [x] U-17 部材の編集：メモ
- 担当：ui-dev ／ 依存：S-04 ／ 仕様書：5.2, 5.4
- やること：部材の編集にメモ欄（複数行、ふつうのキーボードで入力、任意）を足す。逃げの欄が無いことを確かめる
- 完了の条件：側板にメモ「切り出したあとに穴あけ」を入れて保存し、再読み込みしても残る。部材の編集に逃げの欄が無い。幅375pxで横スクロールが出ない

### [x] U-18 寸法表：メモと加工のチェック
- 担当：ui-dev ／ 依存：S-04, U-17 ／ 仕様書：9（寸法表・加工のチェック・メモ）
- やること：部材カードにメモを出す。「仕上がり 済」「木取り 済」のチェックを部材ごとに置き（44px 以上）、チェックした段の数字をグレーにする（「済」の文字も出す）
- 完了の条件：見本の棚板で「木取り 済」を押すと、木取り 873×390 の段だけがグレーになり、仕上がりの段は色のまま。もう一度押すと戻る。再読み込みしても残る。ダーク配色でもグレーと通常の色を見分けられる。メモのある部材はカードにメモが出る

### [x] U-19 木取りの画面：材料を減らせるときのお知らせ
- 担当：ui-dev ／ 依存：E-24, E-25, U-09 ／ 仕様書：9（材料を減らせるときのお知らせ）
- やること：`SavingHints.tsx`。木取りの画面の材料の一覧の上に `findSavingHints` のお知らせを出す。設定は変えない
- 完了の条件：E-24 の例（W445 H1800 D18 ×4枚）の仕事で「切り代を 6mm にすると、シナランバー 18mm が 2 枚減ります（4枚 → 2枚）」が出る。見本では何も出ない。お知らせを見ても設定の切り代は 10 のまま

## 第1.2版（iPhone での2回目の要望）

- 設計は `docs/architecture.md` の 7章。計算ロジック（E）の部分

### 計算ロジック（engine）

### [x] E-26 お知らせの切り代の不具合の調査と、もとに戻らないためのテスト
- 担当：engine-dev ／ 依存：E-25 ／ 仕様書：9（材料を減らせるときのお知らせ）
- やること：「切り代が考えられていない」「切り代を 0 から 5 に変えてもお知らせが変わらない」の原因を engine の側で調べる。4×8 の初期の材料・部材ごとの切り代（空欄／入力あり）・複数の材料・切り方（縦・横・おまかせ）でテストを足す
- 完了の条件：ラワン 4mm・4×8・端切り5・刃厚3・W602 H1200 D4（木目 H）×4枚 で、切り代 5 なら「切り代を 4mm にすると、ラワン 4mm が 1 枚減ります（2枚 → 1枚）」が、3つの切り方すべてで出る。切り代 0 の同じ仕事では切り代のお知らせが出ず、切り代を 5 に変えた仕事では出る。部材の切り代に 5 を入れた部材は変わらないテストが通る

### [x] E-27 逃げ・材料の厚みの表示名から「mm」を外す
- 担当：engine-dev ／ 依存：E-22 ／ 仕様書：4（逃げ）, 5.1
- やること：`nigeName` を「逃げ1」「逃げ0.5」、`boardTokenLabel`（式の表示・ボタン）を「ラワン4」「ラワン2.5」にする。お知らせの文（「ラワン 4mm が 1 枚減ります」）は変えない
- 完了の条件：`nigeName(0.25)` が「逃げ0.25」、`boardTokenLabel` が「ラワン4」、`unitLabel` の表示が `逃げ1`・`ラワン4` になるテストが通る

### [x] E-28 厚みの寸法の不一致をエラーにする
- 担当：engine-dev ／ 依存：E-07 ／ 仕様書：5.3
- やること：枚数1以上で材料のある部材で、厚みの寸法が材料の厚みと合わない（自動で見つからないときを含む）ときは寸法のエラー `thicknessMismatch` にし、木取りから「寸法を計算できない」で除く。保存を止めるための `validatePartForSave(job, part): string[]` を作る
- 完了の条件：シナランバー 18 で W19 H700 D600（自動）が「厚みの寸法が材料の厚み 18 と合いません…」のエラー、厚み W を手で選んだ W19 は「厚みの寸法（W=19）が材料の厚み 18 と合いません」になり、木取りから除かれる。枚数0の行はエラーにならない。`validatePartForSave` がそのメッセージを返し、正しい部材では空になるテストが通る

### [x] E-29 材料の並び順（追加した材料を上、最初からある材料を下）
- 担当：engine-dev ／ 依存：E-18 ／ 仕様書：5.1
- やること：`Board.builtIn` を足し、`defaultBoards` で付ける。`orderedBoards(job)` で、追加した材料を追加した順（保存の並び）に、その下に最初からある材料を並べる。印の無い以前のデータの扱いを architecture.md 7章に書く
- 完了の条件：新しい仕事に シナ 18・シナ 4 の順に足すと、並びが シナ 18・シナ 4・メラミン 1・ラワン 2.5・ラワン 4・ラワン 5.5 になる。印の無い以前のデータでも、最初からある4つと同じ（名前・厚み・大きさ・木目）材料は下に並ぶテストが通る

### 画面（ui）

### [x] U-20 式の入力：エラーの表示でボタンの位置がずれないようにする
- 担当：ui-dev ／ 依存：U-13 ／ 仕様書：5.4
- やること：`FormulaInput.tsx` の式の欄の下に、高さの決まったエラーの場所をとる（ボタンの並びを開いているあいだ）。長いエラーは折り返して、その高さの中に収める（入りきらない分は省く）
- 完了の条件：幅375pxで、W の式を `全体.W −` のようにエラーになる式にしても、ボタンの並びの位置（画面上の y 座標）が変わらない。長いエラーでも横スクロールが出ない

### [x] U-21 式の入力中の自動スクロール
- 担当：ui-dev ／ 依存：U-13 ／ 仕様書：5.4
- やること：W・H・D の式の欄を押してボタンの並びが開いたら、部材の編集シートの中で、式の欄とボタンの並びが画面に収まる位置へスクロールする（動きを減らす設定ならすぐに移る）。iPhone の画面の下の余白（ホームバー）も考える
- 完了の条件：幅375px・高さ667pxの見本の棚板で、W・H・D のどの式の欄を押しても、式の欄がシートの上の近くに来て、その下にボタンの並びが続いて見える（全部が入る高さなら「完了」ボタンまで見える位置で止まる）。動きを減らす設定ではすぐに移る

### [x] U-22 お知らせを設定の変更ですぐ計算し直す
- 担当：ui-dev ／ 依存：U-19, E-26 ／ 仕様書：9（材料を減らせるときのお知らせ）
- やること：architecture.md 7.1。`SavingHints.tsx` の計算し直しの条件に、設定・部材・材料の変更がはっきり入るようにする。計算し直しているあいだは「計算しています」と出す。部材ごとに切り代を入れた部材は、お知らせで変えていないことを示す。store の側で、設定を変えると新しい仕事になり、お知らせが変わるテストを足す
- 完了の条件：初期の材料の仕事（端切り5・刃厚3）で ラワン4 W602 H1200 D4（木目 H）×4枚・部材の切り代は空欄。切り代 0 ではお知らせが出ず、設定で 5 にして木取りの画面を開くと「切り代を 4mm にすると、ラワン 4mm が 1 枚減ります（2枚 → 1枚）」が出る。0 に戻すと消える

### [x] U-23 寸法表の加工のチェックを「完了」にする
- 担当：ui-dev ／ 依存：U-18 ／ 仕様書：9（加工のチェック）
- やること：寸法表の部材カードの、仕上がりの段と木取りの段のチェックの文言を「完了」にする
- 完了の条件：見本の棚板で、仕上がりの段・木取りの段それぞれに「完了」のチェックがあり、押すとその段だけグレーになる

### [x] U-24 逃げ・材料の厚みの表示から「mm」を外したのに画面を合わせる
- 担当：ui-dev ／ 依存：E-27 ／ 仕様書：4（逃げ）, 5.1
- やること：画面に残る「逃げ1mm」「ラワン4mm」の形の文（逃げの説明の例など）を「逃げ1」「ラワン4」に直す
- 完了の条件：`src/ui` に「逃げ1mm」「ラワン4mm」の形の文が残っていない。設定の逃げの一覧・式のボタンが「逃げ1」「ラワン4」

### [x] U-25 厚みの寸法の不一致をエラーとして出し、部材を保存できないようにする
- 担当：ui-dev ／ 依存：E-28 ／ 仕様書：5.3
- やること：部材の編集で `validatePartForSave` のメッセージを出し、保存（追加）を止める（以前の「確かめてください」の表示は外す）。部材の画面のカードは「厚みが合わない」と出す（「式のエラー」とは分ける）。寸法表でもエラーと分かるように出す
- 完了の条件：シナランバー 18 の部材で W19 H700 D600（厚み 自動）にすると、部材の編集に「材料の厚み 18 と同じ寸法がありません…」が出て保存できない。W18 に直すと保存できる。部材の画面・寸法表でその部材が厚みの不一致と分かる

### [x] U-26 材料の追加：厚みを空欄から始め、入れないと追加できない
- 担当：ui-dev ／ 依存：U-04 ／ 仕様書：5.1
- やること：`BoardEditor.tsx` の新しい材料の厚みを空欄にし、空欄なら「厚みを入れてください」と出して追加しない
- 完了の条件：材料を追加するシートを開くと厚みが空欄。空欄のまま追加を押すとメッセージが出て材料が増えない。4 を入れると追加できる

### [x] U-27 材料の並び順を画面に合わせる
- 担当：ui-dev ／ 依存：E-29 ／ 仕様書：5.1
- やること：部材の編集の材料の選択・式の材料の厚みのボタン・設定の材料の一覧を `orderedBoards(job)` の順にする
- 完了の条件：新しい仕事に シナ 18・シナ 4 の順に足すと、3つの場所すべてで シナ 18・シナ 4・メラミン 1・ラワン 2.5・ラワン 4・ラワン 5.5 の順になる

## 第1.3版（iPhone での3回目の要望）

- 設計は `docs/architecture.md` の 8章。計算ロジック（E）→ 保存（S）→ 画面（U）の順に積む
- 保存データ（`kidori.jobs.v2`）の形は変えない。材料のサイズは今までどおり `Board` に持ち、「この仕事でその材料に選んだサイズ」として扱う（8.2）
- 見本で期待する値（上の表）は、見本の材料が 3×6 のときの値のまま（未決事項 24）

### 計算ロジック（engine）

### [x] E-30 木取り済みの部材を木取りの計算から除く
- 担当：engine-dev ／ 依存：なし ／ 仕様書：8, 9（加工のチェック）
- やること：architecture.md 8.5。`PackingResult.done` を足し、`expandPieces` で枚数1以上・`checks.cut === true` の部材をほかの判定より先に `done` に入れて片にしない。`checks.finished` は関係しない
- 完了の条件：見本（縦切り優先）で棚板の木取りの完了をつけると、シナランバー 18 が 2 枚（1枚目 側板×2、2枚目 天地板×2）になり、`done` に 棚板（4枚）が入って `skipped` には入らない。背板に完了をつけると `materials` にシナベニヤ 4 が無くなる。仕上がりの完了だけでは結果が変わらない。W445 H1800 D18 ×4枚（E-24 の例）に木取りの完了をつけた仕事では `findSavingHints` が空になるテストが通る

### [x] E-31 材料のサイズの比較（3×6 と 4×8）
- 担当：engine-dev ／ 依存：E-30 ／ 仕様書：9（材料のサイズの選択）
- やること：architecture.md 8.6。`src/engine/packing/sizes.ts` の `compareStandardSizes(job, dims)`。材料をすべて 3×6 にした仕事・4×8 にした仕事で `packJob` を1回ずつ呼び、材料ごとに枚数・歩留まり・入らない部材の数を返す。元の仕事は変えない
- 完了の条件：見本（縦切り優先）で シナランバー 18 が 3×6 3枚（71.7%）・4×8 2枚（59.8%。帯詰めの手計算の見込み。違ったら計算の理由を報告する）、シナベニヤ 4 が 3×6・4×8 とも 1枚になる。材料の並びが `packJob` と同じ。計算の前後で仕事のデータが変わらない。部材150枚・おまかせで 1秒以内に終わるテストが通る

### [x] E-32 新しい材料の初期サイズ 4×8 と、並び順の判定を材料名＋厚みだけにする
- 担当：engine-dev ／ 依存：なし ／ 仕様書：5.1, 9（材料のサイズの選択）
- やること：architecture.md 8.2。`defaults.ts` に `defaultSheet()`（4×8・1220×2440・木目 長手方向）。`boards.ts` の `isBuiltInBoard` で、印の無い以前のデータは材料名＋厚みだけで最初からある材料か判定する
- 完了の条件：`defaultSheet()` が 4×8 1220×2440 長手方向で、呼ぶたびに別のオブジェクト。印の無い以前のデータで、ラワン 4 のサイズを 3×6 にしても最初からある材料として下に並ぶ。E-29 のテストがそのまま通る

### 保存（store）

### [ ] S-06 最後に使った設定（ひな形）と、ひな形から新しい仕事を作る
- 担当：ui-dev ／ 依存：E-32 ／ 仕様書：4（設定の引き継ぎ）, 5.1
- やること：architecture.md 8.3。`src/store/template.ts` の `SettingsTemplate`・`defaultTemplate()`・`templateOf(job)`・`sameTemplate(a, b)`。`createJob(name, template)` で設定（逃げの id ごと）と材料（材料名・厚み・印）を深いコピーで写し、材料のサイズは 4×8 にする。`newBoard` の初期サイズを 4×8 にする
- 完了の条件：`defaultTemplate()` が 刃厚3・端切り5・切り代10・縦切り優先・逃げ0.5・1・材料 メラミン1・ラワン2.5・4・5.5。切り代 5・逃げ2 を足し・シナ 18（3×6）を足した仕事の `templateOf` から作った仕事が、切り代 5・逃げ 0.5・1・2・材料5つ（シナ 18 は 4×8、材料の id は新しい）になり、並び順も元と同じ。作った仕事の設定を変えてもひな形と元の仕事が変わらない。材料のサイズだけ違う2つの仕事の `templateOf` が同じになるテストが通る

### [ ] S-07 見本をひな形から作る
- 担当：ui-dev ／ 依存：S-06 ／ 仕様書：4（設定の引き継ぎ）, 10
- やること：architecture.md 8.4。`sampleFromTemplate(template, now)`。見本の材料（シナランバー 18・シナベニヤ 4）が無ければ足し、あればそれを使う。部材の材料をつけ替える。見本の材料は 3×6。寸法 1 の逃げが無ければ足し、棚板の式の逃げをつけ替える
- 完了の条件：`defaultTemplate()` から作った見本が 材料6つ（シナランバー 18・シナベニヤ 4 が上に並ぶ）・逃げ 0.5・1 で、寸法と木取りの結果が「見本で期待する値」の表どおり（ランバー 3枚・ベニヤ 1枚）。シナランバー 18 と切り代 5 が入ったひな形では材料が重ならず（シナランバー 18 は1つ）、部材がその材料を使い、切り代 5 で計算される。逃げ1 を消して逃げ2 だけにしたひな形では 逃げ1 が足され、棚板の仕上がり W が 863 になるテストが通る

### [ ] S-08 ひな形の保存と、設定を変えたときの更新
- 担当：ui-dev ／ 依存：S-06 ／ 仕様書：4（設定の引き継ぎ）
- やること：architecture.md 8.3「保存」「ひな形を更新するとき」。`storage.ts` の `loadTemplate`・`saveTemplate`（キー `kidori.lastSettings.v1`）。`StoreState.template` を足し、reducer の `applyOp` で前後の `templateOf` が違えば更新する。`JobStore` の自動保存でひな形も書く（`canSave` が false なら書かない）
- 完了の条件：仕事 A で刃厚を 2 にすると `state.template` の刃厚が 2 になり、そのあと仕事 B で逃げ3 を足すと ひな形が B の設定（刃厚は B の値）になる。部材の変更・材料のサイズの選択・仕事の名前の変更・仕事の追加や削除ではひな形が変わらない。ひな形を保存して読み込むと同じ内容に戻る。キーが無く仕事も無ければ初期値、キーが無く仕事があれば更新日が一番新しい仕事の設定、壊れた JSON なら初期値になり、例外を投げない。`kidori.jobs.v2` の中身と版は変わらないテストが通る

### [ ] S-09 逃げ・材料の一括削除と、材料のサイズの選択
- 担当：ui-dev ／ 依存：S-06 ／ 仕様書：4（設定画面の一覧）, 5.1, 9（材料のサイズの選択）
- やること：`jobs.ts` に `removeNiges(job, ids)`・`removeBoards(job, ids)`（1回の操作でまとめて消す。材料を使っていた部材の材料は未設定になる）と、確認用に選んだものを使っている部材をまとめて返す関数。`setBoardSize(job, boardId, size)`（3×6・4×8 は寸法が決まり木目は長手方向、自由入力は短辺・長辺・木目）
- 完了の条件：見本で 逃げ0.5・逃げ1 をまとめて消すと逃げが空になり、確認用に［棚板（W）］が返る。シナランバー 18・シナベニヤ 4 をまとめて消すと4部材の材料が未設定になり、確認用に部材名が重ならずに返る。無い id が混ざっていても残りは消える。`setBoardSize` でシナランバー 18 を 4×8 にすると 1220×2440 長手方向になり、自由入力 1000×2000 短手方向も選べ、0 以下の寸法は断るテストが通る

### 画面（ui）

### [x] U-28 設定で、足した逃げの「削除」が効かない不具合を直す
- 担当：ui-dev ／ 依存：なし ／ 仕様書：4（逃げ）
- やること：architecture.md 8.9。まず開発サーバー（幅375px）と iPhone で「逃げを足す → すぐ削除を押す」を再現し、原因を報告に書く。見立て：コードの流れ（`removeNige`・reducer・`NigeEditor`）には誤りが見つからず、①「削除」は同じ場所で確認の形に替わるだけで、足した直後はキーボードが出たままなので、押すとキーボードが閉じて画面が動き、確認が見えない（または最初のひと押しが届かない）、② 確認の「削除する」がキーボードの陰に隠れる、③ 保存を止めている（`canSave` が false）ため再読み込みで戻る、の順に疑う。直し方：追加したら追加の欄の注目を外してキーボードを閉じる、確認の形を見える位置へスクロールして見た目をはっきり変える。store に「足した逃げを消すと消え、保存して読み込んでも戻らない」テストを足す
- 完了の条件：iPhone の幅の開発サーバーで、逃げ2 を足した直後に「削除」→「削除する」を押すと 逃げ2 が一覧から消え、再読み込みしても戻らない。初期の 逃げ0.5 も同じく消せる。足したテストが通る

### [ ] U-29 新しい仕事と見本に、最後に使った設定を引き継ぐ
- 担当：ui-dev ／ 依存：S-07, S-08 ／ 仕様書：4（設定の引き継ぎ）, 10
- やること：仕事の画面の「新しい仕事を作る」を `createJob(name, state.template)`、「見本を追加」を `sampleFromTemplate(state.template)` にする。設定の画面の説明に「新しい仕事には、最後に変えた設定が引き継がれます」を足す
- 完了の条件：仕事 A で切り代を 5・逃げ2 を足し・材料 シナ 18 を足してから新しい仕事 B を作ると、B が切り代 5・逃げ 0.5・1・2・シナ 18 を持つ。B で切り代を 8 にしても A は 5 のまま。再読み込みしてから作った仕事 C は切り代 8。見本を追加すると、ひな形の設定にシナランバー 18・シナベニヤ 4 が足されている

### [ ] U-30 設定の逃げ・材料の一覧を同じ見た目と操作にそろえる
- 担当：ui-dev ／ 依存：U-28, S-09 ／ 仕様書：4（設定画面の一覧）, 5.1
- やること：architecture.md 8.8。`SettingsList.tsx` を作り、逃げと材料の一覧に使う（上に追加の入力、1行ずつ 名前・使っている部材・編集・削除）。材料の編集・追加は材料名と厚みだけにする（`BoardEditor` のサイズ・木目の欄を外す）。材料の並びは `orderedBoards` のまま
- 完了の条件：幅375pxで、逃げと材料の一覧が同じ形（追加の入力が上、各行に「編集」「削除」、高さ 44px 以上）で、横スクロールが出ない。材料の追加・編集にサイズと木目の欄が無く、足した材料は木取りの画面で 4×8 になっている。材料の厚みを空欄のままでは追加できない（U-26 のまま）。使っている部材がある逃げ・材料の削除で部材名入りの確認が出る

### [ ] U-31 逃げ・材料の一括削除
- 担当：ui-dev ／ 依存：U-30 ／ 仕様書：4（設定画面の一覧）
- やること：`SettingsList` に「選んで削除」のモードを足す（各行にチェック、「選んだ◯件を削除」「やめる」）。消す前に、選んだものを使っている部材をまとめて示して確認し、`removeNiges`・`removeBoards` で1回で消す
- 完了の条件：見本で 逃げ0.5・逃げ1 を選んで削除すると「棚板（W）」入りの確認が出て、消すと2つとも無くなり、棚板の W に「削除した逃げを使っています」が出る。材料を3つ選んで消すと3つとも無くなる。「やめる」で何も消えない。チェックの押せる大きさが 44px 以上

### [ ] U-32 木取りの画面：材料のサイズの選択
- 担当：ui-dev ／ 依存：E-31, S-09 ／ 仕様書：9（材料のサイズの選択）
- やること：architecture.md 8.6・8.7。`SheetSizePicker.tsx`。木取りの画面の材料ごとに、3×6・4×8 の必要な枚数と歩留まりを並べ、押して選ぶ（選んでいるものが分かるように）。自由入力を選ぶと短辺・長辺・木目の方向の欄を出し、今の結果の枚数・歩留まりを出す。選ぶと `setBoardSize` で仕事に保存し、結果とお知らせを計算し直す
- 完了の条件：見本で シナランバー 18 に「3×6 3枚 71.7%」「4×8 2枚 59.8%」（E-31 の値）が並び、3×6 が選ばれている。4×8 を押すと配置図・必要な材料が 2 枚になり、再読み込みしても 4×8 のまま。新しい仕事の材料は 4×8 が選ばれている。自由入力 1000×2000 を入れると、その大きさで配置図が描かれる。幅375pxで横スクロールが出ない

### [x] U-33 木取りの画面：木取り済みの部材の一覧
- 担当：ui-dev ／ 依存：E-30 ／ 仕様書：8, 9（加工のチェック）
- やること：`PackingResult.done` を「木取り済み（計算から除いています）」として、部材名・材料・枚数の一覧で出す。計算できない部材・入らない部材の一覧とは分ける
- 完了の条件：見本で寸法表の棚板の木取りの「完了」を押してから木取りの画面を開くと、シナランバー 18 が 2 枚になり、「木取り済み（計算から除いています）」に 棚板 4枚 が出る。完了を外すと 3 枚に戻り、一覧が消える

### [x] U-34 式の材料の厚みのボタンは、その部材で選んでいる材料だけにする
- 担当：ui-dev ／ 依存：なし ／ 仕様書：5.4
- やること：architecture.md 8.7。`FormulaInput` の厚みのボタンを、編集中の部材の下書きで選んでいる材料の1つだけにする。材料を選んでいなければ厚みのボタンを出さない。材料を選び直すとボタンもすぐ変わる
- 完了の条件：初期の材料の仕事で、材料 ラワン 4 を選んだ部材の式のボタンに「ラワン4」だけが出る（メラミン1・ラワン2.5・ラワン5.5 は出ない）。材料をラワン 5.5 に変えると「ラワン5.5」だけになる。材料が未選択なら厚みのボタンが無い。すでに式にある別の材料の厚みは、今までどおり表示・計算される

---

## 未決事項

仕様書に書いていないため、仮の扱いを architecture.md に（暫定）として書いたもの。オーナーの判断で変えられるようにしておく。

1. **歩留まりの計算方法**：分子は木取り寸法の面積か仕上がり寸法の面積か。分母は板全体（910×1820）か耳落とし後か。暫定：木取り寸法 ÷ 板全体。**決定（オーナー）**：木取り寸法の面積 ÷ 板全体の面積
2. **おまかせの比べ方**：歩留まりを「使った板の面積」で割ると、縦・横で必要枚数が同じなら歩留まりも必ず同じになり、比べられない。同じときにどちらを採るか（最後の板の余りが大きいほう＝端材を活かしやすいほう、など）。暫定：縦切り優先。**決定（進行役）**：必要枚数が同じなら、一番大きい端材が大きいほう（端材を活かしやすいほう）を採る。それも同じなら縦切り優先
3. **耳落としと刃厚の関係**：耳落とし5mm に刃厚が含まれるか（使える幅 905 か 902 か）。暫定：含む（905）。**決定（オーナー）**：耳落としの幅は刃厚（ノコ厚）を含む。使える幅は 板の短辺 − 耳落とし（サブロクなら 905）
4. **板の端での刃厚**：片が板の端（左端・上端）にぴったり届く場合に刃厚を差し引くか。暫定：差し引かない（片と片の間だけ刃厚をあける）。**決定（進行役）**：暫定のとおり
5. **部材と板の対応**：仕様書では部材に「材料名」だけを入力するが、板は材料名＋厚みで区別する。同じ材料名で厚みの違う板（例：シナ 18 と シナ 4）があると決まらない。**決定（オーナー）**：部材では「材料名＋厚み」の板を選ぶ（部材は板の id を持つ）
6. **枚数0の行（全体）の入力項目**：材料・厚みの寸法・木目を入力しなくてよいか。**決定（オーナー）**：枚数0なら板・厚みの寸法・木目は未設定でよく、厚みの判定もしない
7. **部材名に使える文字**：`+ - * / ( ) .` や空白を含む名前、数字で始まる名前（例：`2段目棚板`）を許すか。**決定（オーナー）**：`+ - * / ( ) .` と空白を含む名前は使えない（`validatePartName`）。数字で始まる名前は、部材名に `.` が無いため `2段目棚板.W` と数値を区別できるので、計算エンジンでは使える（E-03）
8. **部材名を変えたとき**：ほかの部材の式の参照（`旧名.W`）を自動でつけ替えるか。**決定（オーナー）**：つけ替える（`renamePart`、E-05）
9. **帯の中の並べ始め**：縦切り優先の帯の中で、片を板の下（手前）から並べるか上（奥）から並べるか。仕様書は「右から」だけ。暫定：下から。~~帯の中は手前（図の下側）から詰める~~ → **決定（オーナー・変更 2026-09-26）**：帯は右側から、帯の中は上側から。右上から左下に向かって埋まり、端材は左下に残る
10. **端材として出す大きさ**：どのくらい小さいものまで端材として一覧に出すか（例：幅 50mm 未満は出さない）。暫定：刃厚より大きいものはすべて出す。**決定（進行役）**：幅・長さとも 30mm 以上のものを出す
11. **切る順番の位置の表し方**（**決定（オーナー）**：その時点で残っている板の端から測る）：「右端から◯mm」か「左端から◯mm」か、耳落とし前の端か後の端か。縦の切断は残りの右端から。横の切断は残りの上端から。横切り優先（横長に置く）も同じく 縦は右端から・横は上端から（E-17）
12. **切り代の「選択」の選択肢**：仕様書 4 の「選択または入力」の選択肢（例：0／5／10）が決まっていない。暫定：0・5・10 のボタン＋数値入力。**決定（進行役）**：暫定のとおり
13. **仕事の削除・名前の変更・複製**：仕様書 10 は「保存し、開き直せる」だけ。第1版で削除・名前変更を入れるか。**決定（オーナー）**：第1版で、仕事の削除（確認つき）・名前の変更・コピーを入れる
14. **板より大きい部材**：どう置いても板に入らない部材の扱い（エラー表示のみでよいか）。暫定：「板に入らない部材」として一覧に出し、ほかの部材は計算する。**決定（進行役）**：暫定のとおり
15. **木目の設定と厚みの寸法の食い違い**：厚みの寸法を変えたとき、木目や逃げに面でない軸（例：側板で木目 W）が残った場合の扱い。暫定：木目は「どちらでもよい」に戻し、面でない軸の逃げは無視する。**決定（進行役）**：暫定のとおり
16. **ホーム画面に追加したときのオフライン動作**：電波のない現場で開けるようにするか（Service Worker が必要。パッケージ追加の可能性あり）。**決定（進行役）**：第1版では入れない（第2版以降で検討）

### 第1.1版の未決事項

計画係（planner）が architecture.md 6章で仮に決めたもの。オーナーの判断で変えられる。

17. **逃げの寸法をあとから変えられるか**：仕様書 4 は「登録・削除できる」だけだが、6 は「逃げの寸法を変えたら、参照している式の値も変わること」とある。**決定（進行役）**：変えられる（同じ寸法への変更は不可。名前も「逃げ＋新しい寸法」に変わり、式はついてくる）（U-15）
18. **式で使っている材料・逃げを削除した後の式**：エラーとして残すか、そのときの数値に置き換えるか。**決定（進行役）**：エラーとして残す（「削除した逃げを使っています」）。黙って数値に変わらないので、直す場所が分かる
19. **お知らせで試す端切りの値**：端切り 0（端切りしない）だけでよいか。0 は矩を出さないことになるので、ほかの値（例：3mm）を試したいか。~~決定（進行役）：いまは 0 だけ~~。**決定（オーナー）**：切り代を優先して試し、切り代で減らない材料だけ端切りを試す。どちらも今の値から 1mm ずつ小さくして 1mm まで、最後に 0.5mm を試す（0mm は試さない）。枚数が減る一番大きい値を知らせる。組み合わせは試さない。部材ごとの切り代の上書きは変えない（仕様書 9、E-25）
20. **加工のチェックと寸法の変更**：チェックを付けたあとに部材の寸法を変えたとき、チェックを外すか。**決定（進行役）**：外さない（勝手に消えると困るため）
21. **見本（本棚 W900）の材料**：見本を追加したときに、初期の材料4つ（メラミン・ラワン）も入れるか。**決定（進行役）**：入れない。見本は今のまま シナランバー 18・シナベニヤ 4 の2つ（逃げは 0.5・1 を入れる）
22. **逃げの寸法に 0 を許すか**：**決定（進行役）**：0 より大きい値だけ登録できる

### 第1.3版の未決事項

計画係（planner）が architecture.md 8章で仮に決めたもの。オーナーの判断で変えられる。

23. **この版に上げた直後の「最後に使った設定」**：仕様書 4 は「初めて使うときは初期値」とだけある。すでに仕事がある人が上げた直後（まだ設定を変えていない）に新しい仕事を作ったとき、何を引き継ぐか。暫定：更新日が一番新しい仕事の設定。仕事が1つも無ければ初期値。**決定（進行役）**：暫定のとおり
24. **見本の材料のサイズ**：仕様書 9 は木取り画面のサイズの初期値を 4×8 とするが、見本で期待する値（ランバー 3枚など）は 3×6 の値。暫定：見本の材料は 3×6 にする（見本の説明どおりの結果が出るように）。**決定（進行役）**：暫定のとおり
25. **見本と逃げ1**：見本の棚板は 逃げ1 を式で使う。最後に使った設定に 逃げ1 が無いとき（消した・寸法を変えた）どうするか。仕様書は材料だけ「無ければ足す」とある。暫定：逃げ1 も無ければ足す（無いと見本の棚板がエラーになるため）。**決定（進行役）**：暫定のとおり
26. **サイズの比較を出す材料**：部材の無い材料（初期の メラミン1 など）にもサイズの選択を出すか。暫定：その仕事で木取りする部材がある材料だけに出す（部材が無いと枚数を比べられないため）。**決定（進行役）**：暫定のとおり
27. **木取り済みで、式や厚みにエラーがある部材**：暫定：木取り済みの一覧に出し、計算できない部材の一覧には出さない（計算から除いているため）。寸法表・部材の画面のエラー表示は今のまま。**決定（進行役）**：暫定のとおり
