# kidori 仕様書（第1版）

家具づくりで使う板材（合板など）から、部材を歩留まり良く切り出すための **木取り計算システム**。

## 1. 概要

- 部材の仕上がり寸法を入力すると、パネルソーで切る寸法（木取り寸法）に変換し、定尺板への並べ方・必要な板の枚数・歩留まりを計算する。
- 利用者：自分と職場の人たち
- 利用環境：スマートフォン（iPhone / Android）
  - ブラウザで動く Web アプリとして作る。ホーム画面に追加してアプリのように使える形にする

## 2. 用語

| 用語 | 意味 |
|---|---|
| 板 | 切り出す元の定尺材（例：シナ合板 18mm サブロク） |
| 部材 | 板から切り出すパーツ（例：天板、側板、棚板） |
| 仕上がり寸法 | 家具として完成したときの部材の寸法 |
| 逃げ | 納まりを良くするためのクリアランス。仕上がり寸法から引く |
| 切り代 | 仕上がり寸法に足す、あとで削り仕上げるための余分 |
| 木取り寸法 | パネルソーで切る寸法（仕上がり寸法＋切り代） |
| 刃厚 | 1回切るごとに刃で削れて消える幅 |
| 耳落とし | 板の端を最初に切り落とすこと |
| 歩留まり | 板の面積のうち、部材として使えた面積の割合 |

## 3. 全体の流れ

```
① 部材を入力（寸法は数値または式、逃げ）
      ↓ 式を計算し、逃げを引く
② 仕上がり寸法
      ↓ 切り代を足す
③ 木取り寸法 ……（②③を「寸法表」として単独でも見られる）
      ↓ 材料名＋厚みごとに分けて計算
④ 木取り計算（配置図・切る順番・歩留まり・端材・必要枚数）
```

## 4. 設定（仕事ごと）

| 項目 | 初期値 | 備考 |
|---|---|---|
| 刃厚 | 3mm | 変更できる |
| 耳落とし | 5mm | 変更できる。板を縦長に置いたときの **右側の長辺1辺だけ** 落とす |
| 切り代 | 10mm | 選択または入力できる。部材ごとに上書きできる |
| 切り方 | 縦切り優先 | 縦切り優先 / 横切り優先 / おまかせ から選ぶ |

### 切り方

パネルソーを前提に、すべて **端から端まで一直線に切る（ギロチンカット）** 並べ方にする。

- **縦切り優先**：先に長辺方向に帯状に切り、その帯を横に切り分ける。縦長の端材が残りやすい
- **横切り優先**：先に短辺方向に切り、その後で縦に切り分ける
- **おまかせ**：両方を計算し、歩留まりの良いほうを採用する

## 5. 入力項目

### 5.1 板（材料）

| 項目 | 例 | 備考 |
|---|---|---|
| 材料名 | シナ合板 | |
| 厚み | 18mm | |
| サイズ | サブロク（910×1820） | サブロク / シハチ（1220×2440）/ 自由入力 |

- 材料名＋厚みの組み合わせで板を区別する（例：シナ 18mm と ラワン 18mm は別の板）

### 5.2 部材

| 項目 | 例 | 備考 |
|---|---|---|
| 名前 | 側板 | 式の中で参照に使うため、仕事の中で重複不可 |
| 材料名 | シナ合板 | 板の材料名と対応させる |
| 厚み | 18 | 数値または式 |
| 縦 | 700 | 数値または式 |
| 横 | `天板.縦 - 18` | 数値または式 |
| 枚数 | 2 | |
| 木目 | 縦 | 縦 / 横 / どちらでもよい |
| 逃げ（縦） | 0 | mm。仕上がり寸法の縦から引く |
| 逃げ（横） | 1 | mm。仕上がり寸法の横から引く |
| 切り代 | （空欄） | mm。空欄なら仕事の初期値を使う |

- 部材は10種類以上、合計で100枚を超えることもある前提で作る

## 6. 式

- 縦・横・厚みには、数値の代わりに式を書ける
- ほかの部材の寸法を `部材名.縦`、`部材名.横`、`部材名.厚み` で参照できる
- 使える記号：`+` `-` `*` `/` `( )`
- 例：
  ```
  天板：縦 600、横 900
  側板：縦 700、横 = 天板.縦 - 18
  棚板：横 = 天板.横 - 側板.厚み * 2
  ```
- 参照先の寸法を変えると、参照している部材の寸法も自動で変わる
- 次の場合はエラーとして知らせる
  - 存在しない部材名を参照している
  - 循環参照になっている（A が B を参照し、B が A を参照する など）
  - 計算結果が 0 以下になる

## 7. 寸法の計算

```
仕上がり寸法 = 入力値（式の計算結果） − 逃げ
木取り寸法   = 仕上がり寸法 ＋ 切り代
```

- 切り代は縦・横それぞれに合計で足す（例：仕上がり 600×900、切り代 10mm → 木取り 610×910）

## 8. 木取り計算

- 材料名＋厚みごとに分けて計算する
- 条件
  - 刃厚を切るたびに差し引く
  - 耳落としの分を板の右側の長辺から差し引く
  - 木目の指定を守る（部材の縦を板の長辺方向に合わせる／短辺方向に合わせる／どちらでもよい）
  - 選んだ切り方（縦切り優先・横切り優先・おまかせ）に従う
- 1枚に入りきらない場合は、必要な板の枚数を算出し、2枚目以降にも割り振る
- 部材が100枚を超えても、スマホで数秒以内に結果を出す
  - 数学的に最適な並べ方の保証はしない。実用上良い並べ方を素早く出す

## 9. 結果の表示（すべてスマホで見られること）

- **寸法表**：部材ごとの仕上がり寸法・木取り寸法・枚数
- **配置図**：板ごとに、部材を並べた図（部材名・寸法を表示）
- **切る順番**：パネルソーで切る順番
- **歩留まり**：板ごと・全体の歩留まり（%）
- **端材**：残った端材のサイズ
- **必要な板の枚数**：材料名＋厚みごと

## 10. 保存

- 仕事（設定・板・部材）を保存し、あとで開き直せる
- 第1版では、各自のスマホの中に保存する

## 11. 開発の段階

| 版 | 内容 |
|---|---|
| 第1版 | 部材の入力（式・逃げ・切り代）、寸法表、木取り計算（切り方の選択・配置図・切る順番・歩留まり・端材・必要枚数）、保存 |
| 第2版 | 切り代の比較機能（切り代の値を選び、必要枚数と歩留まりを並べて比べ、良いほうで確定する）、保存データを LINE などで送って共有 |
| 第3版 | 職場で同じデータを共有（サーバーを使う） |

## 12. 未決事項

- 式で `部材名.縦` を参照したときの値は、逃げを引いた後の仕上がり寸法とするか、逃げを引く前の値とするか
- 部材の「縦」と板の向き・木目の関係の確認（「木目 縦」＝部材の縦を板の長辺方向に合わせる、でよいか）
